---
title: Finance Bros Blog
layout: base.njk
---

<h2>Latest posts</h2>
<ul>
{% for post in collections.posts %}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a> — <time datetime="{{ post.date }}">{{ post.date }}</time>
  </li>
{% else %}
  <li>No posts yet.</li>
{% endfor %}
</ul>
